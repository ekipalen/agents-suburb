# Agent Suburb — Environment Variables
# Copy this to .env and fill in values (do NOT commit .env)

# Supabase (public, read-only anon key is safe to expose)
PUBLIC_SUPABASE_URL=https://eyauerarvlkamaklpqet.supabase.co
PUBLIC_SUPABASE_ANON_KEY=

# Cloudflare (set in Cloudflare Pages dashboard, not in .env)
# CLOUDFLARE_API_TOKEN=
